[package]
name = "codec"
version = "0.0.1"
edition = "2018"
publish = false

[dependencies]
libc = "0.2"
byteorder = "1.2"
quick-error = "1.2"
panic_hook = { path = "../panic_hook" }
tikv_alloc = { path = "../tikv_alloc", default-features = false }

[dev-dependencies]
protobuf = "2.0"
bytes = "0.4.10"
rand = "0.5"
