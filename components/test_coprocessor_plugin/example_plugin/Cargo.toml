[package]
name = "example_coprocessor_plugin"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[lib]
crate-type = ["dylib"]

[dependencies]
coprocessor_plugin_api = { workspace = true }
