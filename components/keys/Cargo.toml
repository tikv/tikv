[package]
name = "keys"
version = "0.1.0"
edition = "2018"
publish = false

[dependencies]
byteorder = "1.2"
derive_more = "0.15.0"
hex = "0.3"
kvproto = { git = "https://github.com/pingcap/kvproto.git" }

[dev-dependencies]
panic_hook = { path = "../panic_hook" }
