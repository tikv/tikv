[package]
name = "keys"
version = "0.1.0"
edition = "2018"
publish = false

[dependencies]
byteorder = "1.2"
kvproto = { git = "https://github.com/pingcap/kvproto.git" }
log_wrappers = { path = "../log_wrappers", default-features = false }
thiserror = "1.0"
tikv_alloc = { path = "../tikv_alloc", default-features = false }

[dev-dependencies]
panic_hook = { path = "../panic_hook", default-features = false }
