[package]
name = "fuzzer-honggfuzz"
version = "0.0.1"
edition = "2021"
publish = false
license = "Apache-2.0"

[dependencies]
fuzz-targets = { path = "../targets" }

[target.'cfg(not(target_os = "windows"))'.dependencies]
honggfuzz = "0.5.47"
