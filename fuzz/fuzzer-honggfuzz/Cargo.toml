[package]
name = "fuzzer-honggfuzz"
version = "0.0.1"
publish = false

[dependencies]
fuzz-targets = { workspace = true }

[target.'cfg(not(target_os = "windows"))'.dependencies]
honggfuzz = "0.5.47"
